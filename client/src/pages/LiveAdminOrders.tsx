import { useEffect, useState } from 'react';
import { BarChart3, CalendarDays, CircleDollarSign, PawPrint, Search, Settings, Sparkles, TrendingUp, Users, WalletCards } from 'lucide-react';
import { Link } from 'react-router-dom';
import { api } from '../services/api';

type Order={id:number;serviceType:string;date:string;time:string;note?:string;status:string;paymentStatus:string;totalPrice:number;user?:{name:string};pet?:{name:string};payment?:{method:string;amount:number;status:string}};
const statusText:Record<string,string>={PENDING:'Chờ xác nhận',CONFIRMED:'Đã xác nhận',IN_PROGRESS:'Đang phục vụ',COMPLETED:'Hoàn thành',CANCELLED:'Đã hủy'};
const money=(n:number)=>`${Math.round(n).toLocaleString('vi-VN')}đ`;
const nav=[[BarChart3,'Tổng quan','/admin'],[CalendarDays,'Đơn hàng','/admin/orders'],[Users,'Khách hàng','/admin/customers'],[PawPrint,'Thú cưng','/admin/pets'],[CircleDollarSign,'Doanh thu','/admin/finance'],[WalletCards,'Chi phí','/admin/expenses'],[Sparkles,'Combo','/admin/memberships'],[TrendingUp,'Analytics','/admin/analytics']];

export default function LiveAdminOrders(){
  const [orders,setOrders]=useState<Order[]>([]);const [query,setQuery]=useState('');const [status,setStatus]=useState('Tất cả');const [loading,setLoading]=useState(true);const [selected,setSelected]=useState<Order|null>(null);
  useEffect(()=>{api('/bookings').then(setOrders).catch(()=>setOrders([])).finally(()=>setLoading(false))},[]);
  const shown=orders.filter(x=>(status==='Tất cả'||statusText[x.status]===status)&&`${x.id} ${x.user?.name} ${x.pet?.name} ${x.serviceType}`.toLowerCase().includes(query.toLowerCase()));
  const updateStatus=async(order:Order,next:string)=>{setOrders(old=>old.map(x=>x.id===order.id?{...x,status:next}:x));try{await api(`/bookings/${order.id}`,{method:'PUT',body:JSON.stringify({status:next})})}catch{}};
  return <div className="dash admin-system"><aside><Link className="logo" to="/"><span><PawPrint/></span><b>PAWFECT<small>CARE CENTER</small></b></Link><p className="role">QUẢN TRỊ VIÊN</p>{nav.map(([Icon,label,path]:any)=><Link className={`side-link ${label==='Đơn hàng'?'active':''}`} to={path} key={label}><Icon/>{label}</Link>)}<div className="side-bottom"><Link className="side-link" to="/admin/settings"><Settings/> Cài đặt hệ thống</Link></div></aside><div className="dash-main"><div className="dash-top"><div className="dash-search"><Search/>Tìm kiếm trong hệ thống...</div></div><div className="dash-content"><div className="welcome admin-head"><div><p>VẬN HÀNH</p><h1>Quản lý đơn hàng</h1><span>Đồng bộ trực tiếp từ giỏ hàng: combo, số lượng, VAT, thanh toán và trạng thái.</span></div></div><div className="admin-filter"><div><Search/><input placeholder="Tìm mã đơn, khách hàng, thú cưng..." value={query} onChange={e=>setQuery(e.target.value)}/></div><select value={status} onChange={e=>setStatus(e.target.value)}><option>Tất cả</option><option>Chờ xác nhận</option><option>Đã xác nhận</option><option>Đang phục vụ</option><option>Hoàn thành</option><option>Đã hủy</option></select></div><div className="panel admin-table"><table><thead><tr><th>Mã đơn</th><th>Khách hàng</th><th>Thú cưng</th><th>Dịch vụ / combo · số lượng</th><th>Tiền dịch vụ</th><th>VAT 8%</th><th>Tổng cộng</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody>{shown.map(order=>{const subtotal=Math.round(order.totalPrice/1.08);return <tr key={order.id}><td><b>PF-{String(order.id).padStart(5,'0')}</b></td><td>{order.user?.name||'Khách hàng'}</td><td>{order.pet?.name||'—'}</td><td>{order.serviceType}</td><td>{money(subtotal)}</td><td>{money(order.totalPrice-subtotal)}</td><td><b>{money(order.totalPrice)}</b></td><td><select className="order-status" value={order.status} onChange={e=>updateStatus(order,e.target.value)}><option value="PENDING">Chờ xác nhận</option><option value="CONFIRMED">Đã xác nhận</option><option value="IN_PROGRESS">Đang phục vụ</option><option value="COMPLETED">Hoàn thành</option><option value="CANCELLED">Đã hủy</option></select></td><td><button onClick={()=>setSelected(order)}>Chi tiết</button></td></tr>})}</tbody></table>{loading&&<div className="admin-empty">Đang tải đơn hàng...</div>}{!loading&&!shown.length&&<div className="admin-empty">Chưa có đơn hàng. Đơn khách vừa thanh toán sẽ xuất hiện tại đây.</div>}</div></div></div>{selected&&<div className="modal" onClick={()=>setSelected(null)}><div className="admin-detail-modal" onClick={e=>e.stopPropagation()}><button onClick={()=>setSelected(null)}>×</button><span className="eyebrow">CHI TIẾT ĐƠN HÀNG</span><h2>PF-{String(selected.id).padStart(5,'0')}</h2><div><p><span>Khách hàng</span><b>{selected.user?.name}</b></p><p><span>Thú cưng</span><b>{selected.pet?.name}</b></p><p><span>Dịch vụ / combo</span><b>{selected.serviceType}</b></p><p><span>Lịch hẹn</span><b>{selected.time} · {selected.date}</b></p><p><span>Tiền dịch vụ</span><b>{money(selected.totalPrice/1.08)}</b></p><p><span>VAT 8%</span><b>{money(selected.totalPrice-selected.totalPrice/1.08)}</b></p><p><span>Tổng thanh toán</span><b>{money(selected.totalPrice)}</b></p><p><span>Phương thức</span><b>{selected.payment?.method||'Chưa ghi nhận'}</b></p><p><span>Ghi chú</span><b>{selected.note||'Không có'}</b></p></div><div className="admin-detail-actions"><button className="btn primary" onClick={()=>setSelected(null)}>Hoàn tất</button></div></div></div>}</div>;
}
